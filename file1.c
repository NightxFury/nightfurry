#include <stdio.h>

int main(){
    int row,sopace,star;
    printf("Enter the number of rows for the pyramid:");
    scanf("%d",&row);
     for(int i = 1; i<= row; i++);
     //print spaces
     for(sopace = 1);


}