//wap in c to swap the values of variable a & b

//#include <stdio.h>

//int main() {
  //  int a, b,temp;

    //printf("Enter the value of a:");
    //scanf("%d", &a);

    //printf("Enter the value of b:");
    //scanf("%d", &b);

    //printf("Before swapping: a = %d, b = %d\n", a,b);

    //sawp the values

    //temp = a;
    //a = b;
    //b = temp;

    //printf("after swapping: a = %d, b = %d\n", a,b);
    //return 0;

//}